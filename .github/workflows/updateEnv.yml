name: Update Environment Variables in GitHub Secrets

on:
  push:
    branches:
      - dev  # Adjust branch as needed

jobs:
  update-secrets:
    runs-on: ubuntu-latest

    steps:
      # Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Install GitHub CLI
      - name: Install GitHub CLI
        run: |
          sudo apt-get update
          sudo apt-get install -y gh

      # Authenticate with GitHub CLI
      - name: Authenticate with GitHub CLI
        env:
          GITHUB_TOKEN: ${{ secrets.G_TOKEN }}
        run: |
          gh auth status || { echo "Authentication failed"; exit 1; }

      # Update secrets from application.properties
      - name: Update Secrets from application.properties
        env:
          APP_PROPERTIES_PATH: "./src/main/resources/application.properties" # Update this path
        run: |
          if [ ! -f "$APP_PROPERTIES_PATH" ]; then
            echo "Error: application.properties file not found at $APP_PROPERTIES_PATH"
            exit 1
          fi

          # Read and process variables from application.properties
          while IFS='=' read -r key value; do
            # Skip empty lines or comments
            if [ -n "$key" ] && [ -n "$value" ] && [[ $key != \#* ]]; then
              # Check if the secret already exists
              if gh secret list | grep -q "$key"; then
                echo "Updating existing secret: $key"
                echo -n "$value" | gh secret set "$key"
              else
                echo "Adding new secret: $key"
                echo -n "$value" | gh secret set "$key"
              fi
            fi
          done < "$APP_PROPERTIES_PATH"
